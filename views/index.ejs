<%- include('partials/header.ejs')%>
<%- include('partials/homenavbar.ejs')%>
<body>
   <hr>
  <div class="container">
     <hr>
     <div class="jumbotron">
      <h1 class="display-4">Bem vindo</h1>
      <p class="lead">Este é um blog destinado a pessoas interessadas em programação e TI em geral.</p>
      <hr class="my-4">
      <a class="btn btn-primary btn-lg" href="#" role="button">Sobre o BloTech</a>
    </div>
    <hr>

      <% articles.forEach(article => { %>
         <div class="card-header">
            <h2><%= article.title %></h2>
         </div>
         <div class="card-body">
            <a href="#" class="btn btn-success">Ler artigo</a>
         </div>
         <br>
      <% }) %>
  </div>
</body>
<%- include('partials/footer.ejs')%>